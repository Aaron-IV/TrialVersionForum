# 🗨️ Forum

Современный веб-форум, построенный на Go с использованием SQLite. Проект реализует полнофункциональную систему для обсуждений с поддержкой постов, комментариев, реакций и категорий.

![Go Version](https://img.shields.io/badge/Go-1.24-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## ✨ Возможности

- 🔐 **Аутентификация пользователей** - регистрация и вход с безопасным хешированием паролей
- 📝 **Создание и редактирование постов** - пользователи могут создавать, редактировать и удалять свои посты
- 💬 **Комментарии** - возможность комментировать посты с поддержкой редактирования и удаления
- ❤️ **Реакции** - система лайков и дизлайков для постов и комментариев
- 🏷️ **Категории** - организация постов по категориям (General, Programming, Tech, Science, Offtopic)
- 🔍 **Фильтрация** - просмотр своих постов и понравившихся постов
- 🎨 **Современный UI** - Pinterest-стиль интерфейса с адаптивным дизайном
- 🔒 **Безопасность** - защита от CSRF, безопасные заголовки, валидация данных
- 🐳 **Docker поддержка** - легкий запуск через Docker с автоматической синхронизацией базы данных

## 🛠️ Технологии

- **Backend**: Go 1.24
- **Database**: SQLite3
- **Frontend**: HTML5, CSS3, JavaScript
- **Authentication**: Session-based с bcrypt хешированием
- **Security**: Secure headers, method override, input validation

## 📋 Требования

- Go 1.24 или выше
- SQLite3
- Docker (опционально, для контейнеризации)

## 🚀 Установка и запуск

### Локальная установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd forum
   ```

2. **Установите зависимости:**
   ```bash
   go mod download
   ```

3. **Запустите приложение:**
   ```bash
   go run main.go
   ```

4. **Откройте в браузере:**
   ```
   http://localhost:8080
   ```

### Запуск через Docker

#### Использование скрипта docker.sh (рекомендуется)

1. **Сделайте скрипт исполняемым (если нужно):**
   ```bash
   chmod +x docker.sh
   ```

2. **Соберите и запустите контейнер:**
   ```bash
   ./docker.sh rebuild
   ```
   
   Это автоматически:
   - Соберет Docker образ
   - Создаст директорию `data/` если её нет
   - Скопирует базу данных из корня в `data/` (если существует)
   - Запустит контейнер с правильными настройками

3. **Другие команды:**
   ```bash
   ./docker.sh build      # Собрать образ
   ./docker.sh run        # Запустить контейнер
   ./docker.sh stop       # Остановить контейнер
   ./docker.sh restart    # Перезапустить контейнер
   ./docker.sh logs       # Посмотреть логи
   ./docker.sh help       # Показать справку
   ```

#### Ручной запуск через Docker

1. **Соберите Docker образ:**
   ```bash
   docker build -t forum:latest .
   ```

2. **Создайте директорию для базы данных:**
   ```bash
   mkdir -p data
   # Если база данных существует в корне, скопируйте её:
   cp forum.db data/forum.db 2>/dev/null || true
   ```

3. **Запустите контейнер:**
   ```bash
   docker run -d \
     --name forum \
     -p 8080:8080 \
     -v "$(pwd)/data:/app/data" \
     forum:latest
   ```

   **Важно:** Флаг `-v "$(pwd)/data:/app/data"` необходим для сохранения базы данных на хосте.

4. **Проверьте логи:**
   ```bash
   docker logs forum
   ```

## ⚙️ Конфигурация

Приложение использует переменные окружения для конфигурации:

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `FORUM_PORT` | Порт сервера | `8080` |
| `FORUM_DB_NAME` | Путь к базе данных | `forum.db` |
| `FORUM_SESSION_HOURS` | Время жизни сессии (часы) | `24` |
| `FORUM_COOKIE_SECURE` | Безопасные cookies (HTTPS) | `false` |

## 📁 Структура проекта

```
forum/
├── config/              # Конфигурация приложения
│   └── config.go
├── internal/
│   ├── auth/           # Аутентификация и авторизация
│   ├── database/        # Работа с базой данных
│   ├── handlers/        # HTTP обработчики
│   ├── middleware/      # Middleware (auth, logger, secure headers)
│   ├── models/          # Модели данных
│   ├── server/          # Настройка сервера
│   └── templates/       # HTML шаблоны
├── static/              # Статические файлы (JS, CSS, favicon)
├── data/                # База данных для Docker (создается автоматически)
├── docker.sh            # Скрипт для управления Docker контейнером
├── main.go             # Точка входа
├── Dockerfile          # Docker конфигурация
└── go.mod              # Зависимости Go
```

## 🎯 Основные функции

### Регистрация и вход
- Регистрация с email и username
- Вход по email или username
- Безопасное хеширование паролей (bcrypt)
- Управление сессиями

### Посты
- Создание постов с заголовком и содержимым
- Редактирование и удаление своих постов
- Привязка к одной или нескольким категориям
- Валидация категорий (проверка существования)
- Пагинация

### Комментарии
- Комментирование постов
- Редактирование и удаление своих комментариев
- Вложенные комментарии

### Реакции
- Лайки и дизлайки для постов и комментариев
- Отображение количества реакций
- Ограничение: один пользователь - одна реакция

### Категории
- Предустановленные категории: General, Programming, Tech, Science, Offtopic
- Сортировка категорий по ID
- Фильтрация постов по категориям
- Валидация категорий при создании поста (проверка существования)
- Отображение категорий в навигации и на карточках постов

## 🔒 Безопасность

- **Secure Headers** - защита от XSS и других атак (CSP, X-Frame-Options, etc.)
- **Session Management** - безопасное управление сессиями с автоматической очисткой
- **Password Hashing** - bcrypt для безопасного хранения паролей
- **SQL Injection Protection** - использование prepared statements
- **Input Validation** - валидация всех входных данных (длина, формат, существование)
- **Method Override** - безопасная поддержка PUT/DELETE методов

## 🧪 Тестирование

```bash
go test ./...
```

## 📝 API Endpoints

| Метод | Путь | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/` | Главная страница с постами | Нет |
| GET | `/login` | Страница входа | Нет |
| POST | `/login` | Авторизация | Нет |
| GET | `/register` | Страница регистрации | Нет |
| POST | `/register` | Регистрация нового пользователя | Нет |
| GET | `/logout` | Выход | Да |
| GET | `/create_post` | Форма создания поста | Да |
| POST | `/create_post` | Создание поста | Да |
| GET | `/post/:id` | Просмотр поста | Нет |
| PUT | `/edit_post` | Редактирование поста | Да |
| DELETE | `/delete_post` | Удаление поста | Да |
| POST | `/post/:id/comment` | Создание комментария | Да |
| PUT | `/edit_comment` | Редактирование комментария | Да |
| DELETE | `/delete_comment` | Удаление комментария | Да |
| GET | `/like_post` | Лайк/дизлайк поста | Да |
| GET | `/like_comment` | Лайк/дизлайк комментария | Да |

### Валидация данных

При создании поста:
- **Заголовок**: 1-256 символов
- **Содержимое**: 1-2500 символов
- **Категории**: минимум одна категория обязательна, все категории должны существовать в базе данных (валидация возвращает 400 ошибку при несуществующих категориях)
- **Комментарии**: 1-500 символов

## 📌 Важные замечания

### База данных в Docker

При использовании Docker:
- База данных хранится в директории `data/forum.db` на хосте
- Скрипт `docker.sh` автоматически копирует базу данных из корня проекта в `data/`, если она существует
- Все изменения в базе данных сохраняются между перезапусками контейнера благодаря volume mount

### Миграции базы данных

Приложение автоматически применяет миграции базы данных при запуске:
- Добавление недостающих колонок
- Синхронизация данных
- Создание индексов
- Создание стандартных категорий

## 🐛 Известные проблемы

- Нет поддержки HTTPS (только HTTP)
- Нет поддержки загрузки изображений
- Нет поддержки уведомлений

## 🔮 Планы на будущее

- [ ] Поддержка HTTPS
- [ ] Загрузка изображений
- [ ] Система уведомлений
- [ ] Поиск по постам
- [ ] Темная тема
- [ ] Мобильное приложение

## 📄 Лицензия

MIT License

## 👥 Авторы

Разработано с ❤️ на Go

## 🤝 Вклад в проект

Приветствуются любые вклады! Пожалуйста, создайте issue или pull request.

---

**Приятного использования! 🚀**

